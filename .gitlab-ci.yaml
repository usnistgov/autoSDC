image: python:latest

before_script:
  - pip install mkdocs
  - pip install mkdocs-material mkdocstrings

# build mkdocs documentation
pages:
  stage: deploy
  script:
  - mkdocs build
  - mv site public
  artifacts:
    paths:
    - public
  only:
  - master
  tags:
  - shared
  - docker
